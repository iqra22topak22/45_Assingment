"use strict";
let userNames = ['admin', 'iqra', 'sadaf', 'komal', 'salma'];
if (userNames.length === 0) {
    console.log('we need to find some users!');
}
else {
    userNames = [];
    console.log('all user names have been remove.  ' + userNames.length);
}
